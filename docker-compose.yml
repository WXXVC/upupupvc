version: "3.9"
services:
  app:
    image: ${APP_IMAGE:-ghcr.io/wxxvc/upupupvc:latest}
    pull_policy: always
    ports:
      - "9988:9988"
    volumes:
      - ./data.db:/app/data.db
      - ./downloads:/app/downloads
