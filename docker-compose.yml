version: "3.9"
services:
  app:
    image: ${APP_IMAGE:-ghcr.io/wxxvc/upupupvc:latest}
    pull_policy: always
    ports:
      - "9988:9988"
    environment:
      APP_DATA_DIR: /data
    volumes:
      - ./data:/data
      - ./downloads:/app/downloads
